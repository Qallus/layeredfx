'use client';
import {MaterialFamilyPicker} from './material-family-picker';
import {useEffect,useState} from 'react';
type Material={id:string;name:string;sku:string;family:string;source:string;swatch:string;image:string;canApply:boolean};
export function WilsonartLibrary({onSelect}:{onSelect:(name:string,finish:string,color:string,image?:string)=>void}){const [family,setFamily]=useState(''),[q,setQ]=useState(''),[page,setPage]=useState(0),[data,setData]=useState<{items:Material[];families:string[];total:number}>({items:[],families:[],total:0}),[loading,setLoading]=useState(false),[error,setError]=useState(''),[applying,setApplying]=useState(''),[selected,setSelected]=useState<Material|null>(null);useEffect(()=>{const controller=new AbortController();setLoading(true);setError('');const timer=setTimeout(()=>{fetch('/api/studio/materials?'+new URLSearchParams({q,family,page:String(page)}),{signal:controller.signal}).then(r=>{if(!r.ok)throw Error();return r.json();}).then(setData).catch(e=>{if(e.name!=='AbortError')setError('Could not load materials. Try another search.');}).finally(()=>{if(!controller.signal.aborted)setLoading(false);});},150);return()=>{clearTimeout(timer);controller.abort();};},[q,family,page]);async function apply(m:Material){setApplying(m.id);setError('');try{const img=new Image();img.src=m.image;await img.decode();onSelect('Wilsonart '+m.name+' '+m.sku,'custom','',m.image);setSelected(m);}catch{setError('This full sheet is unavailable. Try another design.');}finally{setApplying('');}}
return <section><h3>Wilsonart design library</h3><MaterialFamilyPicker value={family} families={data.families} onChange={value=>{setFamily(value);setPage(0);}}/><label>Design name or SKU<input aria-label="Design name or SKU" value={q} onChange={e=>{setQ(e.target.value);setPage(0);setSelected(null);}} placeholder="Search 2,526 entries"/></label><p role="status">{loading?'Loading designs...':data.total+' matching entries'}</p>{error&&<p role="alert">{error}</p>}<div className="ws-library-grid" aria-busy={loading}>{!loading&&data.items.map(m=><button className="ws-material-card" key={m.id} aria-pressed={selected?.id===m.id} disabled={!m.canApply||!!applying} onClick={()=>void apply(m)}><img src={m.swatch} alt="" loading="lazy" style={{width:'100%',height:85,objectFit:'cover'}}/><b>{m.name}</b><small>{m.sku} / {m.family.replace(' (HPL)','')}</small>{applying===m.id&&<small role="status">Loading material...</small>}{!m.canApply&&<small>Product photo only</small>}</button>)}</div><div className="ws-pagination"><button disabled={!page||loading} onClick={()=>{setPage(page-1);setSelected(null);}}>Previous</button><button disabled={(page+1)*24>=data.total||loading} onClick={()=>{setPage(page+1);setSelected(null);}}>Next</button></div><p className="ws-note">Wilsonart images. Confirm physical samples, product suitability and availability. The supplied catalog includes sheet materials and some product-only entries.</p></section>}
