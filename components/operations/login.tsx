"use client";
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {Button} from '@/components/layeredfx/ui/button';
export function LoginScreen(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);useEffect(()=>{let active=true;fetch('/api/operations/auth/refresh',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'}).then(r=>{if(r.ok&&active)location.assign('/admin');}).catch(()=>{});return()=>{active=false;};},[]);return <main className="lfx ops-login"><div className="ops-login-card"><p className="ops-eyebrow">LAYEREDFX / OPERATIONS</p><h1>Welcome back.</h1><p>Sign in with an account added to your LayeredFX team.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{const res=await fetch('/api/operations/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const data=await res.json();if(!res.ok)throw new Error(data.message||'Sign-in failed.');location.assign('/admin');}catch(e){setError(e instanceof Error?e.message:'Sign-in failed.');}finally{setBusy(false);}}}><label className="ops-field"><span>Email</span><input required name="email" type="email" autoComplete="username"/></label><label className="ops-field"><span>Password</span><input required name="password" type="password" autoComplete="current-password"/></label>{error&&<p className="ops-error" role="alert">{error}</p>}<Button disabled={busy} type="submit">{busy?'Signing in…':'Sign in'}</Button></form><p><Link href="/">Return to the website</Link></p></div></main>;}
