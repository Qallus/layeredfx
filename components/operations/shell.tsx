"use client";
import {useEffect,useState} from 'react';
import {DashboardNav} from './dashboard-nav';
import Image from 'next/image';
import {brand} from '@/lib/brand';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {ArrowUpRight,X,PanelLeftClose,PanelLeftOpen} from 'lucide-react';
import {QuickActions} from './quick-actions';
import {DashboardTopbar} from './topbar';
import {MobileBottomNav} from './mobile-bottom-nav';
import {useOperations} from './provider';
export function OperationsShell({children}:{children:React.ReactNode}){const path=usePathname();const {actor,mode,state,busy,error,notice,reload,clearError}=useOperations();const [mobile,setMobile]=useState(false);const [collapsed,setCollapsed]=useState(false);useEffect(()=>{setCollapsed(localStorage.getItem('lfx:dashboard:sidebar-collapsed')==='true');},[]);const [theme,setTheme]=useState('light');useEffect(()=>{setTheme(localStorage.getItem('lfx:dashboard:theme')||'light');},[]);useEffect(()=>{const onTheme=(e:Event)=>{const next=(e as CustomEvent<string>).detail;if(next==='dark'||next==='light'){setTheme(next);localStorage.setItem('lfx:dashboard:theme',next);}};window.addEventListener('lfx:dashboard-theme',onTheme);return()=>window.removeEventListener('lfx:dashboard-theme',onTheme);},[]);return <div className={`lfx ops ${theme==='dark'?'dark':''} ${collapsed?'ops-sidebar-collapsed':''}`} data-role={actor.role}><aside id="dashboard-sidebar" className={`ops-sidebar ${mobile?'is-open':''}`}><Link href="/admin" className="ops-brand" aria-label="LayeredFX dashboard"><Image className="ops-brand-full" src={brand.dashboard.dark} alt="LayeredFX" width={519} height={72} priority style={{width:"100%",height:"auto"}}/><Image className="ops-brand-icon" src={brand.app} alt="" width={36} height={36}/><small>THE OPERATIONS STUDIO</small></Link><button className="ops-mobile-close" aria-label="Close navigation" onClick={()=>setMobile(false)}><X/></button><DashboardNav path={path} collapsed={collapsed} pipelineCount={state.deals.filter(d=>!d.archivedAt).length} onNavigate={()=>setMobile(false)}/><div className="ops-sidebar-bottom"><p>Beautiful work.<br/><b>Thoughtfully organized.</b></p><button type="button" className="ops-sidebar-collapse" aria-controls="dashboard-sidebar" aria-expanded={!collapsed} aria-label={collapsed?'Expand sidebar':'Collapse sidebar'} title={collapsed?'Expand sidebar':'Collapse sidebar'} onClick={()=>{const next=!collapsed;setCollapsed(next);localStorage.setItem('lfx:dashboard:sidebar-collapsed',String(next));}}>{collapsed?<PanelLeftOpen size={18}/>:<PanelLeftClose size={18}/>}<span className="ops-nav-text">Collapse sidebar</span></button><Link href="/" aria-label="View website" title="View website"><span className="ops-nav-text">View website</span> <ArrowUpRight size={15}/></Link><small>{mode==='demo'?'Local demonstration':'LayeredFX data connection'}</small></div></aside>{mobile&&<button className="ops-nav-backdrop" aria-label="Close navigation" onClick={()=>setMobile(false)}/>}<div className="ops-app"><DashboardTopbar theme={theme} status={busy?'Saving…':notice} onOpenNav={()=>setMobile(true)} onToggleTheme={()=>{const next=theme==='dark'?'light':'dark';setTheme(next);localStorage.setItem('lfx:dashboard:theme',next);}}/>{mode==='demo'&&<div className="ops-demo-banner"><b>LOCAL DEMO</b> Synthetic records. Changes remain local to this development setup. No messages, payments or bookings are sent.</div>}{error&&<div className="ops-global-error" role="alert"><p>{error}</p><div><button onClick={()=>void reload()}>Reload latest data</button><button onClick={clearError}>Dismiss</button></div></div>}<main className="ops-main" aria-busy={busy}>{children}</main><footer className="ops-app-footer">LayeredFX Operations<span>{mode==='demo'?'Preview data':'Authenticated data'} · Revision {state.revision}</span></footer></div><QuickActions/><MobileBottomNav/></div>;}
