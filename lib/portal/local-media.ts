function database(){return new Promise<IDBDatabase>((resolve,reject)=>{const request=indexedDB.open('lfx-portal-media',1);request.onupgradeneeded=()=>request.result.createObjectStore('files');request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(Error('Device storage unavailable.'));});}
export async function localMedia(id:string,file?:Blob){const db=await database();return new Promise<Blob|undefined>((resolve,reject)=>{const tx=db.transaction('files',file?'readwrite':'readonly');const store=tx.objectStore('files');const request=file?store.put(file,id):store.get(id);tx.oncomplete=()=>{db.close();resolve(file||request.result);};tx.onerror=()=>{db.close();reject(Error('Could not access saved media.'));};});}
