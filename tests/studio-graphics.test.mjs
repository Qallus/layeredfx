import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import ts from 'typescript';const code=ts.transpileModule(readFileSync(new URL('../lib/studio/graphics.ts',import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.ESNext}}).outputText;const {decorate}=await import('data:text/javascript;base64,'+Buffer.from(code).toString('base64'));
test('graphics reset material transform before positioning layers',()=>{const calls=[];const ctx={setTransform:(...v)=>calls.push(['transform',...v]),save(){},restore(){},translate:(...v)=>calls.push(['translate',...v]),rotate(){},fillText(){}};decorate({getContext:()=>ctx},[{id:'a',kind:'text',text:'Hello',color:'#000',x:50,y:25,size:70,rotation:0,opacity:1,hidden:false}],'');assert.deepEqual(calls,[['transform',1,0,0,1,0,0],['translate',500,250]]);});
