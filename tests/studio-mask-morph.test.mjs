import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import ts from 'typescript';
const code=ts.transpileModule(readFileSync(new URL('../lib/studio/mask-morph.ts',import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.ESNext}}).outputText;const {morphAlpha}=await import('data:text/javascript;base64,'+Buffer.from(code).toString('base64'));
test('edge growth does not cascade across the mask or wrap between rows',()=>{const pixels=new Uint8ClampedArray(5*5*4);pixels[(2*5+2)*4+3]=255;morphAlpha(pixels,5,5,true);assert.equal(pixels.filter((_,i)=>i%4===3).filter(v=>v===255).length,5);assert.equal(pixels[3],0);});
test('shrink removes perimeter and leaves the interior; empty selection stays empty',()=>{const pixels=new Uint8ClampedArray(3*3*4).fill(255);morphAlpha(pixels,3,3,false);assert.equal(pixels.filter((_,i)=>i%4===3).filter(v=>v===255).length,1);assert.equal(pixels[4*4+3],255);const empty=new Uint8ClampedArray(16);morphAlpha(empty,2,2,true);assert.ok(empty.every(v=>v===0));});
