import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import ts from 'typescript';const code=ts.transpileModule(readFileSync(new URL('../lib/studio/wall-polygon.ts',import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.ESNext}}).outputText;const {validWallPolygon}=await import('data:text/javascript;base64,'+Buffer.from(code).toString('base64'));const points=rows=>rows.map(([x,y])=>({x,y}));
test('drawn areas accept concave outlines in either direction',()=>{const p=points([[.1,.1],[.9,.1],[.5,.5],[.9,.9],[.1,.9]]);assert.ok(validWallPolygon(p));assert.ok(validWallPolygon(p.slice().reverse()));});
test('drawn areas reject crossing edges, repeats, insufficient points and invalid coordinates',()=>{assert.equal(validWallPolygon(points([[.1,.1],[.9,.9],[.1,.9],[.9,.1]])),false);assert.equal(validWallPolygon(points([[.1,.1],[.9,.1],[.9,.1]])),false);assert.equal(validWallPolygon(points([[.1,.1],[.9,.9]])),false);assert.equal(validWallPolygon(points([[NaN,.1],[.9,.1],[.9,.9]])),false);});
